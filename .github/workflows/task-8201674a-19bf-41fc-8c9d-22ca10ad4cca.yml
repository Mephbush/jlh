name: Task 8201674a-19bf-41fc-8c9d-22ca10ad4cca

# Trigger on manual dispatch and schedule
on:
  workflow_dispatch:
    inputs:
      runCount:
        description: 'عدد مرات تشغيل المهمة (Number of runs)'
        required: false
        default: '1'
        type: string

  schedule:
    - cron: '0 */6 * * *'

jobs:
  run-task:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install Dependencies
        run: |
          npm install --save-dev playwright
          npx playwright install chromium
      
      - name: Create Results Directory
        run: mkdir -p results
      
      - name: Run Task Script
        run: node scripts/8201674a-19bf-41fc-8c9d-22ca10ad4cca.js
        env:
          RUN_COUNT: ${{ inputs.runCount || '1' }}
          TASK_ID: 8201674a-19bf-41fc-8c9d-22ca10ad4cca
          TASK_NAME: صحة النشر التلقائي القديم
          TARGET_URL: 
          STEALTH_LEVEL: undefined
          STEALTH_HIDE_WEBDRIVER: undefined
          STEALTH_RANDOM_UA: undefined
          STEALTH_RANDOM_VIEWPORT: undefined
          STEALTH_BLOCK_WEBRTC: undefined
      
      - name: Upload Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: task-results-run-${{ github.run_number }}
          path: results/
          retention-days: 7
      
      - name: Generate Summary Report
        if: always()
        run: |
          echo "## 📊 Task Execution Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Task:** صحة النشر التلقائي القديم" >> $GITHUB_STEP_SUMMARY
          echo "**Task ID:** 8201674a-19bf-41fc-8c9d-22ca10ad4cca" >> $GITHUB_STEP_SUMMARY
          echo "**Workflow:** task-8201674a-19bf-41fc-8c9d-22ca10ad4cca.yml" >> $GITHUB_STEP_SUMMARY
          echo "**Triggered:** ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Run Count:** ${{ inputs.runCount || '1' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -d results ]; then
            echo "**Results:** ```" >> $GITHUB_STEP_SUMMARY
            find results -type f -name "*.json" | head -5 | while read f; do
              echo "- $(basename $f)" >> $GITHUB_STEP_SUMMARY
            done
            echo "```" >> $GITHUB_STEP_SUMMARY
          fi
